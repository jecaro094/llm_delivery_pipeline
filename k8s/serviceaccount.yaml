apiVersion: v1
kind: ServiceAccount
metadata:
  name: model-workload
  namespace: confidential-models
# The producer and consumer never call the Kubernetes API: they only read a
# mounted Secret file and environment variables, so no token is projected
# into either pod.
automountServiceAccountToken: false
